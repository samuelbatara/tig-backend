server:
  port: 8100

spring:
  application:
    name: "tigbook"
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgresql
    url: jdbc:postgresql://localhost:5432/data
    username: samuel
    password: samuel
    driverClassName: org.postgresql.Driver
