server:
  port: 8001

spring:
  application:
    name: "tiguse"
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    platform: postgresql
    url: jdbc:postgresql://localhost:5432/data
    username: samuel
    password: samuel
    driverClassName: org.postgresql.Driver
